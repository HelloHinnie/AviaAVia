[gd_scene format=3 uid="uid://c4c3x6tv4o2s"]

[sub_resource type="Shader" id="Shader_jrm1u"]
code = "shader_type canvas_item;

uniform float amplitude : hint_range(0.0, 0.5) = 0.1;
uniform float frequency : hint_range(0.0, 20.0) = 10.0;
uniform float speed : hint_range(0.0, 10.0) = 2.0;

void fragment() {
    vec4 tex_color = texture(TEXTURE, UV);
    
    // Calcula a onda baseada no eixo X e no tempo
    float wave = sin(UV.x * frequency + TIME * speed) * amplitude;
    
    // Define o limite de preenchimento (ajustado pela onda)
    // Se o UV.y for maior que o preenchimento + a onda, fica transparente
    float fill_limit = 1.0 - COLOR.a; // Usamos o alpha original ou um uniform de progresso
    
    if (UV.y < 0.5 + wave) {
        COLOR = tex_color;
    } else {
        COLOR = vec4(0.0); // Torna invisível o que estiver fora da \"onda\"
    }
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_p0ym1"]
shader = SubResource("Shader_jrm1u")
shader_parameter/amplitude = 0.1
shader_parameter/frequency = 10.0
shader_parameter/speed = 2.0

[node name="ProgressBar" type="Node2D" unique_id=602572879]

[node name="CanvasLayer" type="CanvasLayer" parent="." unique_id=166085510]

[node name="TextureProgressBar" type="TextureProgressBar" parent="CanvasLayer" unique_id=1900884951]
material = SubResource("ShaderMaterial_p0ym1")
offset_left = 32.0
offset_top = 680.0
offset_right = 1240.0
offset_bottom = 1314.0
